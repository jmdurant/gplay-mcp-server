{
  "mcpServers": {
    "google-play": {
      "command": "bash",
      "args": ["-c", "set -a && source $HOME/.env && source ./.env 2>/dev/null && exec node $HOME/gplay-mcp-server/dist/index.js"]
    }
  }
}
